apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: ram1uj/notes-app-user-service
          env:
            - name: DB_HOST
              value: mysql
            - name: DB_NAME
              value: notes_db
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              value: password
          ports:
            - containerPort: 8200